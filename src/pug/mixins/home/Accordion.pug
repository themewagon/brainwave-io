mixin AccordionItem({heading, body}, index) 

  .accordion-item 
    h2.accordion-header(id=`heading${index+1}`)
      button.accordion-button.fs-8.lh-lg.fw-bold(type='button' class=`${index!== 0 ? 'collapsed': ''}` data-bs-toggle="collapse" data-bs-target=`#collapse${index+1}` aria-expand=`${index=== 0 ? true: false}` aria-controls=`collapse${index+1}` data-accordion-button)=  heading 

    .accordion-collapse.collapse(id=`collapse${index+1}` class=`${index === 0 ? 'show': ''}` data-bs-parent="#accordion")
      .accordion-body.fs-9.lh-xl.accoridion-text.opacity-70=  body 
mixin Accordion 
  -
    var items = [
      {
        heading: 'How to setup Shade Pro?',
        body: 'Enjoy the easy setup process with little to no coding knowledge with us!'
      },
      {
        heading: 'Can I use Shade Pro for my clients?',
        body: 'Yes, you can. Enjoy special rates for your first client!'
      },
      {
        heading: 'How often do you release update?',
        body: 'We will keep updating the theme; stay with us to get updated!'
      },
      {
        heading: 'How can I access to old version?',
        body: 'Go to your cloud storage and provide the password. You’ll find it there..'
      }
    ]
  
  #accordion.accordion.mt-lg-3
    each item, index in items 
      +AccordionItem(item, index)